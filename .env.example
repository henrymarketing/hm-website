# Contact form (Resend)
RESEND_API_KEY=re_xxxxxxxx
# Optional — verified sender on your Resend domain
RESEND_FROM="henry.marketing <henry@henry.marketing>"

# Voice Mail — AI Email Dictation Tool
VENICE_API_KEY=
# SMTP password per client — pattern: CLIENT_SMTP_[CLIENTID_UPPERCASE]
# Only needed for clients with smtp_host configured in lib/voiceClients.ts
# CLIENT_SMTP_PIETROBON=   ← add when Pietrobon SMTP is set up
